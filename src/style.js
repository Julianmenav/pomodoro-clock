const containerColors = [ "#abe963", "#b5cf22", "#0077ff", "#58f4ff", "#ffef62", "#ff7474"]
export const containerStyle = {
  backgroundColor: containerColors[Math.floor(Math.random() * containerColors.length)]
}